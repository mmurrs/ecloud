{
  "name": "@layr-labs/ecloud-cli",
  "version": "0.0.0-development",
  "type": "module",
  "files": [
    "dist",
    "bin",
    "VERSION",
    "README.md"
  ],
  "bin": {
    "ecloud": "./bin/run.js"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.10.1",
    "@layr-labs/ecloud-sdk": "workspace:*",
    "@napi-rs/keyring": "^1.0.5",
    "@oclif/core": "^4.8.0",
    "axios": "^1.13.2",
    "chalk": "^5.6.2",
    "cli-table3": "^0.6.5",
    "dockerode": "^4.0.9",
    "form-data": "^4.0.5",
    "handlebars": "^4.7.8",
    "jose": "^6.1.2",
    "js-yaml": "^4.1.1",
    "node-forge": "^1.3.2",
    "open": "^11.0.0",
    "viem": "^2.38.6"
  },
  "scripts": {
    "build": "tsup",
    "build:dev": "BUILD_TYPE=dev tsup",
    "prepublishOnly": "cp ../../README.md .",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "ecloud": "node bin/run.js"
  },
  "oclif": {
    "bin": "ecloud",
    "commands": "./dist/commands",
    "dirname": "",
    "topicSeparator": " ",
    "topics": {
      "auth": {
        "description": "Manage authentication with private keys stored in OS keyring"
      },
      "billing": {
        "description": "Manage billing and subscriptions"
      },
      "compute": {
        "description": "Manage compute projects and resources"
      },
      "compute:app": {
        "description": "Manage projects"
      },
      "compute:app:profile": {
        "description": "Manage app profiles"
      },
      "compute:app:configure": {
        "description": "Configure app settings"
      },
      "compute:environment": {
        "description": "Manage deployment environment [alias: env]"
      },
      "compute:build": {
        "description": "Verifiable builds (build from git source + provenance verification)"
      },
      "compute:env": {
        "hidden": true,
        "description": "Manage deployment environment [alias: env]"
      }
    }
  },
  "devDependencies": {
    "@types/form-data": "^2.5.2",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^18",
    "ts-node": "^10.9.2"
  }
}
